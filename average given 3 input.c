#include<stdio.h>
int main()
{
    int i,num,a,b,c,sum=0;
    float avg;
    scanf("%d",&num);
    for(i=1;i<=num;i++)
    {
       sum=a+b+c;
       avg=(float)sum/3;
    }

    printf("%d\n",avg);
    return 0;
}

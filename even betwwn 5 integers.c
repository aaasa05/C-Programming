#include<stdio.h>
int main()
{
    int i,n,sum=0;

    for(i=1; i<=n; i++)
    {
scanf("%d",&n);
       if(i%2==0)
        {
          sum=sum+i;
            printf("%d valores pares",sum);
        }

    }
}

